{
  "name": "stack-deploy",
  "version": "0.0.0",
  "description": "Deploy the zetta stack",
  "main": "cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.6.0",
    "request": "^2.53.0"
  }
}
